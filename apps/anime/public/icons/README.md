# PWA icons

Place generated install icons in the platform-specific folders:

- `android/`: launcher icons used by the manifest and notifications
- `ios/`: Apple touch icons
- `windows/`: Windows tile and store assets